---
import Button from '@/components/ui/Button.astro';
---

<section class="relative overflow-hidden py-24">
  <!-- Background -->
  <div class="gradient-hero absolute inset-0"></div>
  <div
    class="absolute inset-0 bg-[radial-gradient(circle_at_30%_70%,hsl(var(--primary)/0.15),transparent_50%)]"
  >
  </div>

  <div class="relative z-10 container mx-auto px-4">
    <div class="mx-auto max-w-3xl text-center">
      <!-- Terminal Badge -->
      <div
        class="bg-code border-border mb-8 inline-flex items-center gap-2 rounded-full border px-4 py-2"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="16"
          height="16"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="text-primary"
          ><polyline points="4 17 10 11 4 5"></polyline><line x1="12" x2="20" y1="19" y2="19"
          ></line></svg
        >
        <code class="text-muted-foreground font-mono text-sm">
          <span class="text-primary">echo</span> "Ship faster. Sleep better. Spend less."
        </code>
      </div>

      <h2 class="mb-6 text-3xl font-bold md:text-5xl">
        Looking for an <span class="text-primary">SRE</span> who keeps delivery calm?
      </h2>

      <p class="text-muted-foreground mx-auto mb-10 max-w-2xl text-lg">
        I help teams reduce toil, harden pipelines, and cut cloud waste without slowing down
        delivery. If you need a steady hand on reliability, automation, or platform
        modernization—let's talk.
      </p>

      <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
        <Button href="https://calendly.com/joshuagilman/30min" size="lg" class="group">
          Schedule an Intro
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="ml-2 h-4 w-4 transition-transform group-hover:translate-x-1"
            ><path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path></svg
          >
        </Button>
        <Button href="/resume" variant="outline" size="lg"> View Resume </Button>
      </div>
    </div>
  </div>
</section>
